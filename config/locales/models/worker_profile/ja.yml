ja:
  activerecord:
    models:
      worker_profile:
    errors:
      worker_profile:
        preset_type: Developer type is required.
        max_count_type: You may select up to four categories of work.
        wrong_size: "is the wrong size (should be %{file_size})"
        size_too_small: "is too small (should be at least %{file_size})"
        size_too_big: "is too big (should be at most %{file_size})"
        past_performance1: is dumplicated
