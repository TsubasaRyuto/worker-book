<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1.0">
    <meta name="description" content="WorkerBook - ITフリーランスのためのクラウドソーシングサービス。ITフリーランサーは、自分のスキルや経験を登録して公開するだけです。企業は条件にあった人材を探し、仕事の依頼をすることができます。">

    <%= csrf_meta_tags %>
    <%= include_gon(init: true) %>
    <%= stylesheet_link_tag    'application', media: 'screen', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag('/favicon.ico') %>
    <%= analytics_init if Rails.env.production? %>
    <%= render 'layouts/shim' %>
  <head>

  <body>
    <%= render 'layouts/header' %>
    <section clas="main-body">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>" >
          <%= message %>
        </div>
      <% end %>
      <%= yield %>
    </section>
    <%= render 'layouts/footer' %>
  </body>
</html>
