<header class="intro">
  <div class="navbar navbar-fixed-top navbar-inverse" role="navigation">
    <div class="container">
      <div class="row">
        <div class="navbar-header">
          <h1><%= link_to 'WorkerBook', root_path, id: 'top' %></h1>

          <button type="button" class="navbar-toggle cllapsed" data-toggle="collapse" data-target="#header-navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>

          <div class="collapse navbar-collapse" id="header-navbar" role='navigation'>
            <ul class="nav navbar-nav navbar-right">
              <% if signed_in? %>
                <li class="dropdown">
                  <a href="#" calss="dropdown-toggle" data-toggle="dropdown">
                    <% if user_type(current_user) == 'worker' %>
                      <span class="glyphicon glyphicon-menu-hamburger"></span><%= current_user.username %>
                    <% elsif user_type(current_user) == 'client' %>
                      <span class="glyphicon glyphicon-menu-hamburger"></span><%= current_user.company_name %>
                    <% end %>
                  </a>
                  <ul class="dropdown-menu">
                    <% if user_profile_present? %>
                      <li><%= link_to "Profile", "/#{user_type(current_user)}/#{current_user.username}" %></li>
                      <li><%= link_to "Settings", "/#{user_type(current_user)}/#{current_user.username}/settings/account" %></li>
                    <% else %>
                      <li><%= link_to "Delete Account", "/#{user_type(current_user)}/#{current_user.username}/retire" %></li>
                    <% end %>
                  </ul>
                  <li>
                    <%= link_to sign_out_path do %>
                      <span class="glyphicon glyphicon-log-out"></span>Sign out
                    <% end %>
                  </li>
                </li>
              <% else %>
                <li>
                  <%= link_to sign_in_path do %>
                    <span class="glyphicon glyphicon-log-in"></span>Sign in
                  <% end %>
                </li>
                <li>
                  <%= link_to sign_up_path do %>
                    <span class="glyphicon glyphicon-pencil"></span>Sign up
                  <% end %>
                </li>
              <% end %>
            </ul>
          </div>
      </div>
    </div>
  </div>
</header>
